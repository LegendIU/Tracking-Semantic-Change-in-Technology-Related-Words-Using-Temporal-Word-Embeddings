# Models

This directory is for trained period-specific Word2Vec models.

Expected generated files:

```text
word2vec_1990s.model
word2vec_2000s.model
word2vec_2010s.model
word2vec_2020s.model
```

Model binaries can be large and should usually not be committed.
