# Data

Expected local input:

```text
data/raw/corpus.csv
```

Required columns:

- `year`
- `text`

Generated intermediate files can be stored in `data/processed/`.

Large raw and processed datasets should not be committed to Git.
